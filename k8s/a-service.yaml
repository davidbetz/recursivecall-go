apiVersion: v1
kind: Service
metadata:
  labels:
    service.name: a
    application.name: recursivecall-go
  name: a
spec:
  clusterIP: None
  ports:
  - name: headless
    port: 55555
    targetPort: 0
  selector:
    service.name: a
