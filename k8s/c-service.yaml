apiVersion: v1
kind: Service
metadata:
  labels:
    service.name: c
    application.name: recursivecall-go
  name: c
spec:
  clusterIP: None
  ports:
  - name: headless
    port: 55555
    targetPort: 0
  selector:
    service.name: c
