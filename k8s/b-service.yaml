apiVersion: v1
kind: Service
metadata:
  labels:
    service.name: b
    application.name: recursivecall-go
  name: b
spec:
  clusterIP: None
  ports:
  - name: headless
    port: 55555
    targetPort: 0
  selector:
    service.name: b
