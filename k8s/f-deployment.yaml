apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    service.name: f
    application.name: recursivecall-go
  name: f
spec:
  replicas: 3
  selector:
    matchLabels:
      service.name: f
      application.name: recursivecall-go
  template:
    metadata:
      labels:
        service.name: f
        application.name: recursivecall-go
    spec:
      containers:
      - env:
        - name: PROCS
          value: "1"
        image: davidbetz/recursivecall-go
        livenessProbe:
          exec:
            command:
            - ./recursivecall
            - check
            - e
          failureThreshold: 3
          periodSeconds: 5
          timeoutSeconds: 10
        name: f
        resources: {}
      restartPolicy: Always
status: {}
