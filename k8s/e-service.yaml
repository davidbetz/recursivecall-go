apiVersion: v1
kind: Service
metadata:
  labels:
    service.name: e
    application.name: recursivecall-go
  name: e
spec:
  clusterIP: None
  ports:
  - name: headless
    port: 55555
    targetPort: 0
  selector:
    service.name: e
