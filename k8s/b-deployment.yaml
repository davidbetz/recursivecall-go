apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    service.name: b
    application.name: recursivecall-go
  name: b
spec:
  replicas: 3
  selector:
    matchLabels:
      service.name: b
      application.name: recursivecall-go
  template:
    metadata:
      labels:
        service.name: b
        application.name: recursivecall-go
    spec:
      containers:
      - env:
        - name: PROCS
          value: "1"
        image: davidbetz/recursivecall-go
        livenessProbe:
          exec:
            command:
            - ./recursivecall
            - check
            - a
          failureThreshold: 3
          periodSeconds: 5
          timeoutSeconds: 10
        name: b
        resources: {}
      restartPolicy: Always
status: {}
